if(state != PS_ATTACK_GROUND && state != PS_ATTACK_AIR){
    if state != PS_IDLE_AIR sprite_index = sprite_get(string(form) + get_state_name(state));
    switch(state){
        case PS_IDLE:
        break;
    }
}